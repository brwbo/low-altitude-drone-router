# Point the frontend at the routing-engine server (run `node server.js` in the
# repo root, then copy this file to .env.local). Unset = pure mock, no backend.
NEXT_PUBLIC_ROUTE_API=http://localhost:3000/route
